apiVersion: apps/v1
kind: Deployment 
metadata:
  name: code-server 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: code-server
  template:
    metadata:
      labels: 
        app: code-server
    spec:
      securityContext:
        fsGroup: 1000
      containers:
      - name: code-server
        image: 454374565233.dkr.ecr.eu-west-2.amazonaws.com/code-server:78cffa8e6c54731b8b612b545d828215f4d78b3c
        ports:
        - containerPort: 8080
        env: 
          - name: PASSWORD
            valueFrom: 
              secretKeyRef:
                name: code-server-secret
                key: PASSWORD
        volumeMounts:
        - name: data 
          mountPath: /home/coder 
      volumes: 
      - name: data
        persistentVolumeClaim:
          claimName: code-server-pvc
